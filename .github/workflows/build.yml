name: Build CandyPad Firmware

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    container: ghcr.io/qmk/qmk_cli

    steps:
      - name: Checkout userspace
        uses: actions/checkout@v4

      - name: Setup QMK
        run: qmk setup -y

      - name: Fetch Binepad keyboard definitions
        run: |
          git clone --depth 1 https://github.com/binepad-global/qmk_userspace_binepad.git /tmp/binepad
          mkdir -p /root/qmk_firmware/keyboards/binepad
          cp -r /tmp/binepad/keyboards/binepad/* /root/qmk_firmware/keyboards/binepad/

      - name: Copy userspace keymap
        run: |
          mkdir -p /root/qmk_firmware/keyboards/binepad/candypad/keymaps/my_keymap
          cp -r $GITHUB_WORKSPACE/keyboards/binepad/candypad/keymaps/my_keymap/* /root/qmk_firmware/keyboards/binepad/candypad/keymaps/my_keymap/

      - name: Compile firmware
        run: |
          cd /root/qmk_firmware
          qmk compile -kb binepad/candypad -km my_keymap

      - name: Upload firmware
        uses: actions/upload-artifact@v4
        with:
          name: candypad-firmware
          path: |
            /root/qmk_firmware/*.hex
            /root/qmk_firmware/*.bin
            /root/qmk_firmware/*.uf2
